name: flutter_ddd
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get_it: ^3.1.0
  provider: 4.0.1
  sqflite: ^1.2.0
  uuid: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  test: 1.9.4

flutter:
  uses-material-design: true
