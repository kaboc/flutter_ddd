name: flutter_ddd
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  uuid: ^2.0.4
  sqflite: ^1.2.0
  provider: ^3.2.0
  get_it: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  test: 1.9.4

flutter:
  uses-material-design: true
